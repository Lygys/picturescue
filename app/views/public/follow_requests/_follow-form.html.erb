<% if current_user.follow_requesting?(user) %>
  <%= form_with model: @follow_request, url: follow_request_path(user), method: :delete, local: true do |f| %>
    <%= f.hidden_field :follow_id, value: user.id %>
    <%= f.submit 'Remove Request', class: 'btn btn-danger btn-block' %>
  <% end %>
<% else %>
  <%= form_with model: @new_follow_request, url: follow_requests_path(user), local: true do |f| %>
    <%= f.hidden_field :follow_id, value: user.id %>
    <%= f.submit 'Follow Request', class: 'btn btn-primary btn-block' %>
  <% end %>
<% end %>